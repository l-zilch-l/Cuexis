# Cuexis 阶段 1C 全量代码审查报告

状态：审查完成；P3/阶段性风险 R17-R21 已修复
审查日期：2026-07-22  
P3 修复日期：2026-07-26
审查版本：`26.07.18.18-1`  
审查范围：当前仓库全部已激活 C++ target、Player、CMake/CTest 门禁、相关测试、ADR 0023/0025/0027/0028/0029、阶段 1B/1C/1E 计划与完成报告  
审查性质：只读代码审查；除创建本报告外未修改产品代码、测试或构建配置

修复注记：本报告的原始问题描述保留为历史证据。2026-07-26 已逐项完成 R17-R21，
并通过 MSVC Release `/WX` 无头 clean build、160 项 CTest、架构扫描以及 add_subdirectory /
install + find_package 两种外部 consumer。Linux 动态分析、静态分析和覆盖率已配置为远程 CI
门禁，仍需首次 push/PR 的托管运行记录。

## 1. 结论

当前未发现 P0 问题，确认 5 项 P1、11 项 P2 和 5 项 P3/阶段性风险。

总体架构方向基本合理：Chart、Runtime、World、Playback、Render 与平台后端的依赖方向清楚；JSON DOM 隔离、事务式 Session、稳定对象 ID、绝对时间 Behavior 求值、有界 Diagnostics、Resource Handle/Lease/Scope 和 target allowlist 都是可继续保留的基础。

但是，当前实现仍属于“仓库内可运行的阶段 1C Playback 原型”，不能视为已经达到 ADR 0027 所要求的外部 SDK 交付状态。最重要的问题是：

1. 阶段 1C 的 Player/Playback 改造实际破坏了阶段 1B 已验收的 Renderable 资源播放闭环。
2. PlaybackSession 自身没有实现 owner-thread 契约，平台和销毁路径又大量依赖仅在 Debug 生效的断言。
3. OpenGL 错误线程销毁会主动放弃 GPU 句柄，形成确定性泄漏。
4. Runtime update 与 Snapshot 提取包含多次全量遍历、排序、分配和字符串复制，无法合理支撑代码允许的 100,000 对象上限。
5. 文件系统 containment 采用“检查路径后再重新打开”的方式，仍存在 reparse/symlink 竞争窗口。

因此，[阶段 1C 完成报告](stage_1c_completion_report.md)中“当前没有已知 P0/P1 实现问题”的结论需要在修复并补充回归门禁后重新评估。

## 2. 严重度定义

```text
P0  数据破坏、任意代码执行、普遍崩溃或无法构建/启动，必须立即停止交付
P1  已确认的核心功能回归、资源/线程安全缺陷、安全边界失效或主要性能阻塞
P2  特定输入/生命周期下的正确性问题、公共契约不一致或明确的维护障碍
P3  当前可绕过，但会削弱质量门禁、可移植性、诊断或后续扩展效率的问题
```

## 3. P1 问题

### 1C-R01：阶段 1B Renderable 资源播放闭环回归

**状态：已修复**

**证据**

- Player 构建 AssetDatabase 后只记录数量，局部变量随后销毁：
  [player_app.cpp:379](../../app/player/src/player_app.cpp#L379)。
- `PlaybackSession::loadChart()` 始终创建无 ResourceManager 的 `RuntimeSession`：
  [playback_session.cpp:117](../../engine/playback/src/playback_session.cpp#L117)。
- Runtime 对任何 Renderable 对象明确要求注入资源绑定：
  [chart_world_instantiator.cpp:230](../../engine/runtime/src/chart_world_instantiator.cpp#L230)。
- 阶段 1B 报告明确记录默认 Player 已支持 3 个 Renderable、3 个 CPU blob 和依赖闭包：
  [stage_1b_completion_report.md:6](stage_1b_completion_report.md#L6)。

**实际复现**

```powershell
.\out\build\debug\bin\cuexis_player.exe `
  --project .\assets\projects\stage1b_project `
  --smoke-test
```

进程在创建 SDL Window 前退出，关键输出为：

```text
Indexed assets: 3
playback.session.prepare_failed
diagnostic_code=runtime.chart.renderable_resources_unsupported
field_path=/objects/0
```

**影响**

- 阶段 1B 已验收能力被阶段 1C 改造破坏。
- Player 的 AssetDatabase 构建成本完全浪费。
- Playback 无法加载仓库内已有的 Renderable Project。
- 阶段 1E 的 Provider/外部消费者工作缺少可迁移的资源闭环基线。

**修复方向**

- Playback 组合层必须在 Session 整个生命周期内拥有或可靠引用 Project source、AssetDatabase、ContentProvider 和 ResourceManager。
- 在正式 1E API 冻结前，可以先增加内部构造配置或 Player 专用工厂，但不能重新让 Player 直接维护私有 RuntimeSession 路径。
- 恢复 `stage1b_project` 的 Player/headless 回归，断言资源数量、Renderable 数量、Scope 数量和 Snapshot/Render 输出。

### 1C-R02：PlaybackSession 未实现 owner-thread 契约

**状态：已修复**

**证据**

- Playback State 没有 ThreadChecker 或 owner identity：
  [playback_session.cpp:66](../../engine/playback/src/playback_session.cpp#L66)。
- `state()` 直接读取 State：
  [playback_session.cpp:88](../../engine/playback/src/playback_session.cpp#L88)。
- `loadChart()` 在无任何线程检查时清空、追加和排序 Diagnostics，并在调用线程创建 RuntimeSession：
  [playback_session.cpp:92](../../engine/playback/src/playback_session.cpp#L92)。
- `update()` 先读取 Playback State，之后才由内部 RuntimeSession 检查线程：
  [playback_session.cpp:138](../../engine/playback/src/playback_session.cpp#L138)。
- 阶段 1C 计划明确要求同一 Session 保持 owner-thread 规则：
  [stage_1c_implementation_plan.md:180](../stage_plans/stage_1c_implementation_plan.md#L180)。

**触发条件**

- Session 在 A 线程创建，在 B 线程首次加载。
- Session 在加载后移动到另一线程使用。
- 宿主在 update/reload/unload 同时读取 state、chartInfo 或 diagnostics。

**影响**

- Playback 自身字段在 Runtime 拒绝调用前就可能发生数据竞争。
- Session owner 实际由“首次创建内部 RuntimeSession 的线程”隐式决定，而不是由 PlaybackSession 契约决定。
- 外部宿主无法可靠判断哪些查询允许跨线程。

**修复方向**

- owner identity 放入 Playback State，并在每个 public API 接触任何状态前检查。
- 明确构造线程是否就是 owner；如需迁移，提供只能在 quiescent/Empty 状态使用的显式 `transferOwnership()`，不要依赖普通 move 隐式迁移。
- 为查询、销毁、move、load 失败和多 Session 增加 Debug/Release 线程测试。

### 1C-R03：OpenGL 错误线程销毁会泄漏，并可能错误线程释放 SDL Window

**状态：已修复**

**证据**

- `OpenGlBackend::release()` 在非 SDL main/owner thread 时记录日志、清零所有 GL 句柄并直接返回：
  [open_gl_backend.cpp:581](../../engine/render_opengl/src/open_gl_backend.cpp#L581)。
- 该分支没有销毁 Context、Program、VAO 或 VBO，也没有投递 deferred destruction。
- `window_` 在该分支没有清空；最后一个 Lease 释放时，`WindowState::~WindowState()` 会调用 `SDL_DestroyWindow`：
  [sdl_window.cpp:201](../../engine/platform/src/sdl_window.cpp#L201)。
- SDL owner 检查最终依赖仅在非 `NDEBUG` 生效的 `assertCurrent()`：
  [thread_checker.cpp:16](../../engine/core/src/thread_checker.cpp#L16)。

**影响**

- Context 和 GPU 对象确定性泄漏。
- Studio 预览重建、热重载、宿主切场景或异常退出路径可能持续积累 GPU 资源。
- Release 下最后一个 Window lease 可能在错误线程执行 SDL API。

**修复方向**

- 增加 owner-thread `close()`/`shutdown()`，返回 `Result` 并要求宿主在帧安全点调用。
- 后端析构只处理已经关闭的对象；未关闭对象通过 owner queue 延迟销毁，或以明确 fatal contract 终止，而不是静默泄漏。
- 对 Context、Window、Program、VAO、VBO 分别记录销毁计数并增加错误线程回归测试。

### 1C-R04：逐帧 Runtime 与 Snapshot 路径无法扩展到默认对象预算

**状态：已修复**

**证据**

- 默认 Chart 上限允许 100,000 个对象：
  [limits.hpp:19](../../engine/chart/include/cuexis/chart/limits.hpp#L19)。
- 每帧 `TransformPropertyResolver::prepare()` 重置全部 Transform baseline，并重新 compose/校验全部 Transform：
  [property.cpp:95](../../engine/world/src/property.cpp#L95)、
  [property.cpp:174](../../engine/world/src/property.cpp#L174)。
- commit 再验证并替换全部 Transform，即使本帧没有对应写入：
  [property.cpp:187](../../engine/world/src/property.cpp#L187)。
- `updateWorldTransforms()` 每帧重新创建实体数组、排序、创建节点数组和 unordered_map：
  [transform_system.cpp:61](../../engine/world/src/transform_system.cpp#L61)。
- Runtime 每次 update 都无条件执行上述完整路径：
  [runtime_session.cpp:482](../../engine/runtime/src/runtime_session.cpp#L482)。
- Snapshot 每帧逐对象执行二分 `findEntity()`、复制 ChartObjectId，再复制到 ObjectSnapshot：
  [playback_session.cpp:166](../../engine/playback/src/playback_session.cpp#L166)。

**影响**

- update 至少包含多次 O(N) 全量扫描和一次 O(N log N) 排序。
- extract 包含 N 次 O(log N) 查找、两轮 ID 字符串复制和新 vector 分配。
- 多 Session、Studio 高频 Seek 和未来 Animation/Particle 扩展会放大 CPU 与内存带宽压力。
- 当前 3 对象 demo 和功能单测无法暴露该问题。

**修复方向**

- prepare 阶段生成稳定的拓扑顺序、父索引、对象/Entity 对齐表和可直接复制的 Snapshot layout。
- Resolver 只重建存在动画绑定或被覆盖的属性；WorldTransform 只更新脏节点及后代。
- Snapshot 使用稳定 ID 表、索引或共享不可变身份，避免逐帧复制字符串。
- 提供调用方缓冲、双缓冲或 arena，并给出 1k/10k/100k 对象的 update/extract 基准和内存峰值。

### 1C-R05：文件系统 containment 存在 TOCTOU 竞争窗口

**状态：已修复**

**证据**

- Asset 读取先执行 `weakly_canonical`、root prefix 和 regular-file 检查：
  [asset_database.cpp:588](../../engine/assets/src/asset_database.cpp#L588)。
- 随后独立查询 `file_size()`，最后才以路径创建 `ifstream`：
  [asset_database.cpp:599](../../engine/assets/src/asset_database.cpp#L599)、
  [asset_database.cpp:617](../../engine/assets/src/asset_database.cpp#L617)。
- Project 和 Player 文件读取也采用路径检查与打开分离的模式：
  [project_loader.cpp:798](../../engine/project/src/project_loader.cpp#L798)、
  [player_app.cpp:148](../../app/player/src/player_app.cpp#L148)。

**触发条件**

有权限修改项目/资产根的并发进程，在 canonical/regular-file 检查与 open 之间把目标替换为 symlink、junction、reparse point 或另一文件。

**影响**

- 可能读取声明 root 之外的数据，违反 ADR 0025 的物理 containment 承诺。
- 即使替换目标仍在 root 内，也可能出现检查对象与实际读取对象身份不一致。
- 当前短读/增长检测不能发现“相同尺寸的替换文件”。

**修复方向**

- Windows 使用 `CreateFileW` 打开句柄后查询 final path/file identity、reparse 属性和大小，再从同一句柄读取。
- POSIX 使用可信 root dirfd、`openat`/`openat2`、`O_NOFOLLOW` 和 `fstat`，避免重新按路径解析。
- 在阶段 1E 集中到 FilesystemContentProvider，增加 symlink/junction swap 与相同尺寸替换测试。

## 4. P2 问题

### 1C-R06：对象级 Camera 校验会静默回退默认值

对象组件解析只在值有效时赋值，没有为非法 projection type、FOV、near/far 生成语义诊断，也没有检查 `near < far`：
[canonical_chart_loader.cpp:499](../../engine/chart/src/canonical_chart_loader.cpp#L499)。顶层 Camera 使用了完整诊断逻辑，两个入口行为不一致。Schema artifact 虽包含部分限制，但 Canonical loader 明确不调用 Schema validator，Compiler 的 `validateComponents()` 也没有补充 Camera 校验。

结果是非法 Chart 可能被静默改写为 `perspective/60/0.1/1000`，或带着反向裁剪面进入 Runtime，直到 extract 时才失败。应让 Reader 报字段级错误，并让 Compiler 对程序化 ChartDocument 再做一次完整语义校验。

### 1C-R07：FrameSnapshot 静默丢弃无 Transform 的合法对象

Runtime 允许只含 Element、Note 或其他非空间组件的对象，并只在 Chart 声明 Transform 时创建 TransformComponent：
[chart_world_instantiator.cpp:276](../../engine/runtime/src/chart_world_instantiator.cpp#L276)。但 Snapshot 只有在 Entity 具有 WorldTransformComponent 时才创建 ObjectSnapshot：
[playback_session.cpp:191](../../engine/playback/src/playback_session.cpp#L191)。

因此 `ChartInfo.objectCount` 与 `FrameSnapshot.objects.size()` 可能不一致，宿主无法区分“对象不存在”和“对象存在但没有 Transform”。需要冻结以下方案之一：所有 Snapshot 对象强制 Transform；ObjectSnapshot 带 `hasTransform`；或明确将集合改名为 spatial/render objects。

### 1C-R08：CameraComponent 投影缓存固定为 aspect=1

实例化对象相机和默认相机时都以 `aspect=1.0` 写入 projectionMatrix：
[chart_world_instantiator.cpp:301](../../engine/runtime/src/chart_world_instantiator.cpp#L301)。FOV 动画更新同样以 `aspect=1.0` 重算：
[runtime_session.cpp:249](../../engine/runtime/src/runtime_session.cpp#L249)。真正的宿主视口只在 Snapshot 提取时参与计算。

当前 Snapshot 输出正确，但 CameraComponent 中的缓存始终可能与真实视口不一致。后续 Studio、RenderSystem 或调试代码一旦直接读取该字段就会得到错误投影。建议移除 ECS 中 viewport-dependent 矩阵，或将缓存放入 RenderFrame/Snapshot 并明确刷新 owner。

### 1C-R09：`core::makePerspective()` 缺少公共输入契约

`makePerspective()` 返回 `Mat4` 且标记 `noexcept`，但不验证 FOV、aspect、near/far 的有限性、正值和顺序：
[math.cpp:171](../../engine/core/src/math.cpp#L171)。非法输入会直接生成 NaN/Inf 或奇异矩阵。

当前调用方存在重复且不一致的前置校验。建议将验证集中为 `Result<Mat4>`，或将未检查实现降为 internal helper，并提供经过验证的公共入口。

### 1C-R10：PlaybackSession moved-from 对象可空指针解引用

move constructor/assignment 会使源对象 `state_ == nullptr`：
[playback_session.cpp:78](../../engine/playback/src/playback_session.cpp#L78)。但 `state()`、`diagnostics()`、`loadChart()`、`unload()` 等全部直接解引用 `state_`。

虽然 moved-from 对象不必保持原值，但无前置条件的 `state() noexcept` 不应产生未定义行为。可选择让 move 后源对象重新获得 Empty State，或删除 move；若保留受限语义，必须在 API 文档和测试中明确。

### 1C-R11：Playback 状态机包含不可到达状态

公开枚举包含 `Paused` 和 `Failed`：
[playback_session.hpp:71](../../engine/playback/include/cuexis/playback/playback_session.hpp#L71)。代码只把 Paused 当作允许输入状态，从未进入 Paused；Failed 没有任何赋值路径。Runtime invariant、update 或 extract 失败后 Session 仍保留旧状态。

这使宿主无法区分可重试的输入错误和不可恢复的 Runtime 失败。需要实现明确的 pause/resume 与 fatal classification，或在冻结前删除未实现状态。

### 1C-R12：公共 callback API 允许异常跨模块边界

`World::withRegistry()` 直接 `std::invoke`：
[world.hpp:28](../../engine/world/include/cuexis/world/world.hpp#L28)。`RuntimeSession::withWorld()` 虽返回 Result，也没有捕获 callback 异常：
[runtime_session.hpp:118](../../engine/runtime/include/cuexis/runtime/runtime_session.hpp#L118)。

这与 CODE_POLICY 中“异常不得跨 Cuexis 模块公共边界”的规则冲突。Runtime/World 若仅作为内部实现，应收窄 public surface；否则需要捕获非 OOM 异常并转换为稳定 Error，同时定义 callback 的重入、阻塞和生命周期规则。

### 1C-R13：Reload 批量失败诊断对宿主不可见

reload 的 Chart load/compile/Runtime diagnostics 保存在局部变量中，失败时 `operationError()` 只复制第一条诊断 code/path：
[playback_session.cpp:278](../../engine/playback/src/playback_session.cpp#L278)。活动 `state_->diagnostics` 必须保留旧 Session 的诊断，因此本次失败的完整集合在返回后丢失。

建议使用 `ReloadResult { reloaded, error, diagnostics }`，或分离 active diagnostics 与 last-operation diagnostics。选择“第一条诊断”时也应查找第一条 Error，而不是无条件使用 `items().front()`。

### 1C-R14：Chart/Behavior IR 存在重复持有和复制

Playback State 保存一份 ChartRuntime：
[playback_session.cpp:66](../../engine/playback/src/playback_session.cpp#L66)。RuntimeSession 内部再次保存 ChartRuntime：
[runtime_session.hpp:175](../../engine/runtime/include/cuexis/runtime/runtime_session.hpp#L175)。prepare 又把 RuntimeBehavior 的 Track/Key 全量复制到 BehaviorProgram：
[runtime_session.cpp:137](../../engine/runtime/src/runtime_session.cpp#L137)。

在默认允许 262,144 Behavior keys、多 Session 和频繁 reload 时，这会产生明显峰值内存与复制成本。建议只保留一份不可变定义 IR；RuntimeEvaluationState 保存 binding/index 和必要的求值缓存；Playback 只保存 Snapshot 所需的稳定对象身份表。

### 1C-R15：RuntimeSession 对 ResourceManager 的生命周期只靠文档约定

构造函数接收 `ResourceManager&`，内部长期保存裸指针：
[runtime_session.cpp:359](../../engine/runtime/src/runtime_session.cpp#L359)、
[runtime_session.hpp:177](../../engine/runtime/include/cuexis/runtime/runtime_session.hpp#L177)。若 Manager 先销毁，现有 Lease 可能仍持有 CPU resource，但后续 prepare/reload 会解引用悬空指针。

RUNTIME_SESSION.md 已说明 Manager 必须活得更久，但类型系统没有表达这一点。阶段 1E 组合时应由 Playback State 同时拥有 Manager 和 Runtime，或让 Runtime 持有可检测过期的 service/weak state，避免外部调用者自行维持隐式顺序。

### 1C-R16：AssetDatabase 的 `maxRoots` 不是严格工作量预算

超过 `maxRoots` 时只添加诊断，但仍按 `input.roots.size()` 分配 `validRoots`、遍历全部 root、执行 canonical/filesystem 检查，并把全部 root push 到内部数组：
[asset_database.cpp:223](../../engine/assets/src/asset_database.cpp#L223)。

最终虽然不会发布数据库，但恶意或程序化输入仍可放大 CPU、内存和文件系统调用。应在记录 root-limit 诊断后将后续处理严格截断到预算，或立即失败；其余输入不应继续产生无界工作。

## 5. P3 与阶段性风险

### 1C-R17：ContentProvider 已声明但未接入

**状态：已修复（2026-07-26）**

新增独立 `cuexis_content` 模块与 Filesystem、Memory、Host 三种 Provider；AssetDatabase
区分 Filesystem/Logical source，ResourceManager 只通过注入或数据库默认 Provider 读取，
PlaybackSession 在整个 Runtime/ResourceScope 生命周期内持有 Provider。Host callback 的异常
与重入被转换为稳定 Error。Content、Asset、Playback 定向测试与架构扫描通过。

`IContentProvider` 当前是孤立公共接口：
[content_provider.hpp:31](../../engine/playback/include/cuexis/playback/content_provider.hpp#L31)。没有 Playback 构造参数、State 成员或 ResourceManager 读取路径使用它。接口在阶段 1E 冻结前仍可调整，但不应让调用者误认为 Host/Memory Provider 已经可用。

### 1C-R18：没有 install/export/find_package 和 external consumer gate

**状态：已修复（2026-07-26）**

已增加 C++20 静态包的 `install(TARGETS ... FILE_SET HEADERS)`、`CuexisTargets.cmake`、
`CuexisConfig.cmake`、精确版本文件、生成版本头和 `Cuexis::Playback` / `Cuexis::Content`
目标。安装树包含项目许可证、THIRD_PARTY_NOTICES 和 5 个无头依赖的实际 vcpkg copyright。
两个隔离 consumer 执行同一 MemoryContentProvider 与 load/update/extract/reload/unload 流程；
find_package 门禁额外扫描安装文件和 Playback 头禁用依赖。两种模式均通过。

根 CMake 与 Playback target 没有 `install()`、export set、CuexisConfig/Version 或 INSTALL_INTERFACE：
[CMakeLists.txt:21](../../CMakeLists.txt#L21)、
[playback/CMakeLists.txt:1](../../engine/playback/CMakeLists.txt#L1)。这属于阶段 1E 已知范围，但在完成前无法验证 public headers、静态库 LINK_ONLY 依赖、许可证和真实仓库外消费。

### 1C-R19：headless 构建仍会无条件查找 SDL/OpenGL

**状态：已修复（2026-07-26）**

已增加 Player、SDL adapter、OpenGL adapter、测试与开发工具开关；顶层保持开发默认值，
作为子项目时默认关闭。`headless-debug` / `headless-release` 禁用 Player 与 adapter，并使用
vcpkg 无默认 feature 的基础依赖闭包。全新无头构建未生成 Player/SDL/OpenGL target，
MSVC Release 98 步 clean build 与 160 项 CTest 通过。

`engine/CMakeLists.txt` 无条件加入 platform 和 render_opengl：
[engine/CMakeLists.txt:1](../../engine/CMakeLists.txt#L1)。对应模块无条件 `find_package(SDL3/glad)`。因此即使消费者只需要 Playback，顶层 configure 仍要求后端依赖。需要阶段 1E 的组件开关和 adapter-disabled preset。

### 1C-R20：进程级日志单例不适合最终嵌入边界

**状态：已修复（2026-07-26）**

已删除 Core 的进程级 logger/init/shutdown，改为实例拥有的 `core::LogSink`；OpenGL config
接收可选 sink，Player 使用应用私有且不注册到 spdlog 全局 registry 的 `PlayerLogger`。
Core 不再链接 spdlog/fmt，重复宿主与多实例生命周期测试通过。

Core 日志使用进程级 mutex/logger/name：
[log.cpp:19](../../engine/core/src/log.cpp#L19)。重复 init 会无条件成功，任一调用方 shutdown 会关闭全局 logger。当前 Playback 不主动初始化它，因此不是当前运行缺陷；但外部 SDK 不应与宿主或多个组件争用全局日志生命周期。阶段 1E 应引入宿主 sink/diagnostic sink，并把 Player 的初始化留在应用层。

### 1C-R21：质量门禁不阻止警告，且缺少跨平台/动态分析/性能门禁

**状态：已修复（2026-07-26）**

Release 与 headless-release 预设强制 warnings-as-errors；恢复 Windows/MSVC Debug/Release CI
并保留 MinGW。新增 Linux/GCC Release、Linux/Clang ASan+UBSan、阻断式 clang-tidy 和 40%
engine 行覆盖率门禁。100k Transform 稀疏更新与 Snapshot buffer reuse 作为确定性性能结构
门禁，墙钟数据仅作趋势。MSVC headless-release `/WX` clean build 与 160 项 CTest 已在本地
通过；Linux 门禁已完成配置校验，待远程 runner 首次执行。

Preset 明确把 `CUEXIS_WARNINGS_AS_ERRORS` 设为 OFF：
[CMakePresets.json:14](../../CMakePresets.json#L14)。CI 当前只有 Windows/MSVC Debug/Release，没有 Clang/GCC、ASan/UBSan、静态分析、覆盖率或性能回归。现阶段 Windows-only 可以接受，但进入外部 SDK 和更多平台前需要补齐；至少 Release CI 应保证项目自身 warning-free。

## 6. 架构评价

### 6.1 可保留的设计

- `cuexis_playback` 作为宿主门面、RuntimeSession/World/EnTT 作为内部实现的方向正确。
- Chart typed Reader 与 JSON DOM 隔离有效，外部输入具有大小、深度、字符串、对象、Key 和诊断预算。
- Chart compile、Runtime prepare/commit/reload 的候选发布与失败回滚结构清楚。
- Behavior 以绝对 chartTimeMs 求值，排序后的 IR 和 discontinuity 契约有利于 Seek 与确定性。
- Resource Handle 的 index/generation/manager token 和 Lease/Scope 生命周期设计整体合理。
- CMake target 依赖 allowlist、架构正则扫描和公共 Playback header 检查能阻止明显的依赖反转。

### 6.2 当前主要结构缺口

```text
Project/Chart source
-> AssetDatabase
-> ContentProvider
-> ResourceManager
-> PlaybackSession
-> RuntimeSession
-> FrameSnapshot
```

当前只实现了 `Chart JSON -> PlaybackSession -> 无资源 RuntimeSession -> FrameSnapshot`。Player 单独完成了 Project/AssetDatabase 的前半段，但没有把结果接入 Playback，导致职责边界在最关键的组合点断开。

Playback State 后续应成为以下对象的统一 owner：

```text
Session owner identity
Host/Memory/Filesystem ContentProvider
AssetDatabase / ResourceManager
RuntimeSession
稳定 Object identity table
active diagnostics / last-operation diagnostics
Snapshot buffer policy
```

Player 只应保留 CLI、Clock、Window、输入和可选 Render/Audio adapter。

## 7. 测试覆盖评价

阶段 1C 报告记录：

```text
Debug CTest       167/167
Release CTest     167/167
Architecture      通过
clang-format      通过
GPU smoke         6/6
远程托管 CI       已配置但无运行记录
```

本次审查没有重新运行完整 configure/build/CTest；只执行了阶段 1B Project 的定向 Player 回归，并确认失败。现有测试数量充足，但以下高风险场景没有形成门禁：

- `stage1b_project` 经正式 PlaybackSession 的 Renderable/ResourceManager 闭环。
- PlaybackSession 创建、load、move、update、query、unload 和 destroy 的跨线程行为。
- Release 下 World/SDL/OpenGL 错误线程销毁。
- moved-from PlaybackSession 的查询、重载和重新赋值。
- 非法对象 Camera type/FOV/near/far 与 `near >= far`。
- 无 Transform Object 的 Snapshot 契约。
- Reload 失败的完整 diagnostics 可见性。
- Filesystem symlink/junction swap、相同尺寸替换和打开后 identity 验证。
- 1k/10k/100k 对象 update/extract 基准与峰值内存。
- adapter-disabled configure、install + find_package 和仓库外 consumer。
- ASan/UBSan、Clang/GCC 或等价动态/跨编译器检查。

### 7.1 P3 修复后验证

2026-07-26 新增并执行：

```text
MSVC headless-release fresh configure       通过
MSVC headless-release clean build + /WX     98/98 build steps
MSVC headless-release CTest                  160/160（1 项 Windows symlink 条件跳过）
MSVC 完整 Debug/Release clean build          113/113，Release 启用 /WX
MSVC 完整 Debug/Release CTest                181/181（同一 symlink 条件跳过）
add_subdirectory external consumer           通过
install + find_package external consumer     通过
安装文件、许可证和 Playback 头扫描           通过
100k Transform 确定性规模门禁                通过（本机 Release 约 0.14 秒，仅作趋势）
默认项目与 Stage 1B 资源项目 GPU smoke        均完成 3 帧；Stage 1B 持有 3 项资源
Windows/MSVC、MinGW、Linux quality workflow  已配置；待远程首次运行记录
```

## 8. 建议修复顺序

### 第一批：恢复功能与所有权

1. 恢复阶段 1B Renderable 资源闭环，并先增加失败的回归测试。
2. 为 PlaybackSession 增加 owner-thread、明确 move/destroy 语义。
3. 建立 OpenGL/SDL owner-thread 显式 shutdown 或 deferred destruction。

### 第二批：正确性与安全

1. 修复对象 Camera 与 Compiler 校验。
2. 冻结无 Transform Object 的 Snapshot 语义。
3. 分离 active/operation diagnostics，落实 Failed 状态分类。
4. 把 Filesystem 读取迁移为基于已打开句柄的 containment 验证。

### 第三批：性能结构

1. prepare 阶段缓存 Transform hierarchy 拓扑和对象/Entity 对齐表。
2. 引入脏 Transform 更新和 Snapshot buffer reuse。
3. 去除重复 Chart/Behavior IR 与逐帧 ID 字符串复制。
4. 建立可重复性能基准和内存预算门禁。

### 第四批：阶段 1E 交付

1. 完成 Filesystem/Memory/Host ContentProvider。
2. 增加 App/adapter/test/install 组件开关。
3. 完成 install/export/package config、公共头扫描和许可证安装。
4. 建立 add_subdirectory 与 find_package external consumer，并与 Player/headless 做确定性对照。

## 9. 审查后的准确状态

```text
模块分层方向                 基本合理
阶段 1C Behavior/Seek 核心   功能测试基础良好
阶段 1B 资源能力回归         已确认，P1
线程与销毁契约               不完整，P1
大规模逐帧性能               高风险，尚无基准
文件系统安全                 有 TOCTOU 缺口
外部 SDK 交付                C++20 静态 Playback 包与 consumer 已完成；完整 1E 仍在实施
工作区产品代码               审查时未修改；R17-R21 修复已于 2026-07-26 落地
```
