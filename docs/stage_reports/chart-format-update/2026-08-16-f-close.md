# Stage Chart Format Update CFU-F Close

状态：CFU-F complete；项目所有者已指示关闭 CFU-F4，并在 CFU-F 全部完成后直接合并至
`stage-ChartFormatUpdate`

快照日期：2026-08-16

实现提交：`8fcac15d2ec053750abdcb7b984d92354bc304a0`
（`Complete CFU-F consumer and safety gates`）；该 SHA 是最终 hosted 验证基线。

权威计划：[Stage Chart Format Update 实施计划](../../stage_plans/completed/chart-format-update/plan.md) §5.8

批次证据：[F1](2026-08-15-f1-headless.md)、
[F2](2026-08-15-f2-package-consumers.md)、
[F3](2026-08-15-f3-determinism.md)、
[F4](2026-08-15-f4-safety-performance.md)

## 1. 结论

CFU-F1–F4 现已关闭。public Playback-only headless/reference 与 external consumer 覆盖 filesystem、
CXC file/memory、typed project-document、prepare options、semantic identity 和 transactional failure；
canonical CXC、migration、identity、FrameDigest v3 与 diagnostics fingerprint 在 hosted GCC、Clang、
MSVC 和 MinGW 上一致；limits、overflow、diagnostic truncation、zero-allocation、sanitizer、clang-tidy、
coverage 和最大内容性能证据均已取得。

项目所有者于 2026-08-16 指示完成后续工作、关闭 CFU-F4，且 CFU-F 全部完成后不创建 PR，直接
合并至 `stage-ChartFormatUpdate`。最终实现 SHA 的三套 hosted workflow 已全部成功，退出门禁满足。

该关闭不是完整 CXC 公共产品支持、公共 package API、完整 v4 动画 Playback、CFU-G、格式阶段完成
或 Stage 4。运行时脚本和逐帧脚本回调继续无限期延后。

## 2. 计划 5.8 退出对照

| 工作包 | 退出条件 | 结果 |
| --- | --- | --- |
| CFU-F1 | 无 GPU static v4 CXC reference；filesystem/CXC source frame/identity parity；失败 reload 原子性 | 已关闭；证据见 F1 报告 |
| CFU-F2 | installed Playback-only external consumer；static/shared、add_subdirectory/find_package、InternalCxc link closure | 已关闭；证据见 F2 报告与 hosted Windows/Linux jobs |
| CFU-F3 | canonical writer/migration/identity/digest/diagnostics 的最终 SHA 跨平台 parity | 已关闭；六份 hosted artifact 完全一致 |
| CFU-F4 | limits/overflow/truncation、zero-allocation、sanitizer、clang-tidy、coverage、最大内容性能 evidence | 已关闭；本地与 hosted evidence 均通过 |

## 3. 本地证据

F1–F4 本地报告保留各批次的历史矩阵。最终 F4 worktree 验证为：

```text
Debug fresh configure + full build                           passed
Debug full CTest                                             379/379 passed
Release fresh configure + clean-first full build with /WX   passed
Release full CTest                                           379/379 passed
CFU-F4 focused tests                                         10/10 passed
Debug / Release maximum-content probes                       passed
cuexis_format_check                                          passed
tools/check_docs.py                                          passed
tools/update_version.py --check                              passed (26.08.01-1)
git diff --check                                             passed
```

## 4. Hosted 验证

最终实现 SHA `8fcac15d2ec053750abdcb7b984d92354bc304a0` 的三套 workflow 全部成功：

| Workflow | Run | Jobs / evidence |
| --- | --- | --- |
| Linux Quality | [31933654969](https://github.com/l-zilch-l/Cuexis/actions/runs/31933654969) | GCC Release、GCC Shared Release、Clang Shared Debug、Clang ASan + UBSan、GCC Coverage、clang-tidy 全部成功；sanitizer job 在 attempt 2 成功 |
| Windows MSVC | [31933654970](https://github.com/l-zilch-l/Cuexis/actions/runs/31933654970) | Debug、Release 首轮成功 |
| Windows MinGW | [31933654968](https://github.com/l-zilch-l/Cuexis/actions/runs/31933654968) | Debug、Release 首轮成功 |

Linux Quality attempt 1 的唯一失败发生在 `Clang ASan + UBSan` 全量 CTest 的 external audio-SDL
consumer 配置：vcpkg 从 util-linux 镜像下载 libmount source 时返回 TLS error 35。该失败在测试代码
执行前发生，不是 sanitizer 或 Cuexis 诊断。同一实现 SHA 的 failed-job rerun 成功：全量
`342/342`、F4 聚焦 sanitizer `9/9`。没有为该重跑修改代码。

## 5. Artifact 核对

六份 F3 artifact（GCC Release、Clang Shared Debug、MSVC Debug/Release、MinGW Debug/Release）都记录
同一 implementation SHA，且以下值完全一致：

```text
fingerprint_sha256=9f7f98fc3bedf81588e62011fd3a49587014b3390aae3906404498e7b60176a0
cxc_sha256=1cb2fcbf7a852a4db2ed9119359c68ff1cc4a06d1d41d18018fff89cf737d723
semantic_identity=6d01494c126f3ae8fc9420259dc92873233022dec9dd6bf9caf04b217f100cc5
stop_frame_digest_v3=11596562486377158370
migration_chart_sha256=1ca9f60feee215fdc4eca1f7cafbbea8704976eca18ed40e51333b6b2e7a5385
migration_report_sha256=81df14e422603ae411ea8a70f5de89fb49ae2a34b804af71146a8be3075824e0
```

F4 hosted evidence：

| Evidence | Result |
| --- | --- |
| ASan + UBSan | full `342/342`；focused F4 `9/9`；无 sanitizer/UBSan finding |
| clang-tidy | `cuexis_chart`、`cuexis_cxc`、`cuexis_playback` closure 构建完成，无诊断 |
| CXC coverage | lines `80.3%`、functions `93.7%`、branches `42.4%` |
| Chart v4 coverage | lines `80.4%`、functions `96.8%`、branches `44.8%` |
| 64 MiB Linux Release trend | write `41.808 MiB/s`、hash-load `62.409 MiB/s`、prepare `347840.991 us`、warmed update+extract `0.773 us` |

性能结果只作为同 SHA 趋势基线，不是跨 runner 的硬阈值。

## 6. 兼容边界

本关闭保持以下边界：

```text
全部 Chart v1/v2/v3 Reader 与迁移入口继续保留
默认 chart_migrator 输出继续为 v3
无公共 Cuexis::Cxc component 或 package API
无 Stage 4 animation sampling、blending 或 AnimationSystem
无 runtime scripts、per-frame callbacks 或 bytecode
FrameSnapshot / FrameDigest v3 公共结构不变
CxcPackageIdentity 不进入 PreparedSemanticIdentity
```

## 7. Handoff

下一批次是 CFU-G：对合并后的 Stage Chart Format Update 最终候选执行验收、封存、完成报告与
Stage 4 typed handoff。CFU-G 完成且经项目所有者接受前，不得宣称完整 CXC、格式阶段完成或开始
Stage 4 动画求值。
