# S6-A1 基线、入口和证据矩阵

状态：completed（S6-A1）；A2 尚未开始

日期：2026-09-20

本报告只关闭 Stage 6 计划中的 S6-A1。它记录本工作区本次执行的基线、现有代码入口、
实际注册测试和支持边界；不把 A2 的合同落盘、v5 Playback 实现、历史 Foundation hosted
结果或 owner 接受写成 A1 的实现证据。

## 1. 执行基线

| 项 | 本次事实 |
| --- | --- |
| 工作区 | `C:/Users/Zilch/.codex/worktrees/7596/Cuexis` |
| HEAD | `e7d6c1ea3ff950226b7a3582eb128cb2d01800fb` |
| 分支 | detached HEAD；本批次未创建分支、未推送、未创建 PR |
| 工作树 | 开工 clean；临时 `cxc_unpack.cpp` 诊断已恢复，最终无源代码 diff |
| UTC | `2026-09-20`；执行期间记录 `2026-09-20T14:50:43Z` |
| 日期版本 | `26.08.01-1`；`tools/update_version.py --check` 通过 |
| SDK API | `0.7.0`；未升级到目标 `0.7.1` |
| 编译器 | MSVC 19.51.36256，x64，VS 18 Community Developer Command Prompt |
| CMake / Ninja | CMake 4.3.3 / Ninja 1.13.2 |
| `VCPKG_ROOT` | `D:/vcpkg` |
| 本次 preset | `debug`、`release`；另检查 Windows headless preset 注册 |

所有构建目录均在本工作区的 `out/build/` 下，没有使用其它工作树的构建产物作为本报告证据。

## 2. R5 支持矩阵

S6-A1 采用 R5 §10 的允许清单，不扩大 candidate revision，也不改变默认格式或 SDK 版本。

| 输入/能力 | 本批次结论 | 入口/归属 | 正例与边界 |
| --- | --- | --- | --- |
| 正式生产回退 | 保留 v4、CXT v1、CXC v1；SDK `0.7.0` | 现有 `PlaybackSource` / `PlaybackSession` | Debug/Release 全量通过；不改默认 Writer 或旧工厂 |
| candidate Packed | 仅 `flags=1`、`candidateRevision=1` | Foundation `packed::decode` / CXC validator | developer-tools=ON 可发现 candidate CXC；裸 Packed 不是用户入口 |
| candidate profile | `candidate.lanes4`、`press`、一个 lane `[0,3]`、tap/point、空 effects | Foundation profile validator | Hold/Release、Slide/Flick、多指、range 和未登记 section 禁止 |
| identity/revision | 保留 Foundation identity 规则；旧 v4 identity 与 FrameDigest v1-v3 不变 | Foundation / 既有 Playback | A2 才定义新的 execution/prepared/config golden |
| runtime scripts | 不支持、不预留字段/capability/hook | 无入口 | 本批次没有实现 |

预算继续沿用 R5 §10.1 的 16 MiB 文件/section/decoded、40,000 实体和 requirements、
100,000 字符串/引用约束。容量数据是指定 profile 的回归基线，不是任意谱面承诺。

## 3. 代码入口盘点

| 责任 | 当前事实 | 后续归属 |
| --- | --- | --- |
| Source | `playback_source.hpp:68,76-80` 只有 `fromChartText`、`fromFilesystemProject`、`fromCxcFile`、`fromCxcMemory` 等既有工厂 | 未发现三个显式 entry 工厂；A2、C1/C4 |
| CXC candidate validation | `tools/cxc_common/src/cxc_candidate.cpp` 校验 registered profile、compiled identity 并调用 Packed decode；candidate CXC tests 仅在 tool layer 存在时加入 | C1 需迁移允许的 typed 能力；Playback 不依赖 tools |
| Packed decode | `packed_chart_io.hpp` 提供 `PackedChartReader::decode`；Foundation 已区分 decode 与 inspect | A1 不增加运行时解析或 CXT 展开；C1 |
| Playback prepare | `playback_session.cpp:1537-1849` 是现有 v1-v4 prepare、RuntimeSession、资源、presentation 和 prepared identity 路径 | candidate source state、Requirement tuple、execution ID 尚不存在；A2/C1 |
| Presentation | 现有 portable presentation 和 `engine/render_opengl` OpenGL path | `cuexis_presentation_renderer` 尚无实现；A2/D1/D2 |
| Player | `player_app.cpp` 直接 include SDL audio/window、Playback、RenderScene、OpenGL；现有 `--chart`、`--project`、`--smoke-test` | 无 candidate explicit entry、统一 command/state、Reference Host；A2/C2/C3/C4 |
| Audio / install | Player 直接链接 `audio_sdl`；既有 CMake package 和 external consumer gates | media importer、device transaction、experimental flavor、Reference Host 未实现；A2/C2/E1/E2/C4 |

本盘点没有把 Catch2 helper、`tests/cfu_f` consumer 或 CXC CLI 误记为公共 Playback 用户入口。
ADR 0042 中的 `cuexis_presentation_renderer`、`cuexis_player_support` 和
`examples/reference_host/` 在当前工作区仍未实现；文档中的同名引用是冻结边界，不是代码存在证据。

## 4. 实际测试注册和结果

### 4.1 Debug / Release

| 配置 | 构建 | CTest 注册 | CTest 结果 | 跳过 |
| --- | --- | ---: | --- | --- |
| Debug | 成功 | 683 | `683/683 passed` | 1 个 symlink containment 用例 |
| Release | `--fresh` + `--clean-first` 成功 | 683 | `683/683 passed` | 1 个 symlink containment 用例 |

两套配置均实际包含 architecture、7 个 external consumer、6 个 CXC、5 个 CFU-F、packed、
playback、headless、package、external 标签组，以及既有 v4、FrameDigest v1-v3、portable
presentation、prepare/reload 相关用例。

执行命令：

```powershell
cmake --preset debug --fresh
cmake --build --preset debug
ctest --preset debug --no-tests=error
cmake --preset release --fresh
cmake --build --preset release --clean-first
ctest --preset release --no-tests=error
```

### 4.2 Candidate CXC 注册边界

Windows headless fresh configure 的实际结果如下：

| 配置 | Developer tools | 注册结果 | candidate 事实 |
| --- | --- | ---: | --- |
| `headless-debug` | OFF | 32 | 输出 `developer tool layer absent; candidate CXC cases excluded`；candidate CXC 不注册 |
| `headless-release` | OFF | 32 | 同上 |
| `headless-shared-debug` | OFF | 35 | 同上；串行指定 VS Ninja 后成功 |
| `headless-shared-release` | OFF | 35 | 同上 |
| `headless-release` | ON | `ctest -N` 总计 93 个测试；`cuexis_cxc_tests` 构建并发现 | 总数包含已发现的 real candidate、out-of-profile rejection、playback entry 正反例 |
| `headless-shared-debug` | ON | `ctest -N` 总计 96 个测试；`cuexis_cxc_tests` 构建并发现 | 总数包含 candidate CXC 用例；共享库配置还有现有额外测试差异 |

Developer tools=ON 只是 Foundation 工具层注册和构建验证，不等于 Stage 6 Playback entry 已实现。
Linux workflow 的 developer ON/OFF、sanitizer、coverage、clang-tidy 仍需 hosted Linux 执行。

### 4.3 门禁

- `cuexis_architecture_tests`：Debug/Release 全量通过。
- Debug/Release 的 7 个 external consumer 全部通过。
- `python -B tools/update_version.py --check`：通过，版本为 `26.08.01-1`。
- `cmake --build --preset debug --target cuexis_format_check`：通过，clang-format 22.1.3。
- 最终文档修改后需再次运行 `python -B tools/check_docs.py`、`git diff --check`。

## 5. 未执行、阻塞与归属

| 项 | 状态 | 归属 |
| --- | --- | --- |
| Linux Quality、GCC/Clang hosted | 待验证；Windows 不替代 hosted | F1 |
| sanitizer / coverage / clang-tidy | 待验证；Linux/Clang/GCC 专属 | F1 |
| GPU/window smoke、真实音频设备、真实宿主 | 待验证；A1 未启动 | D2/C3/C4/F1 |
| experimental source entry / candidate Playback | 未实现；三个显式 entry 工厂不存在 | A2 -> C1/C4 |
| typed lowering / Requirement retention / execution identity | 未实现；当前仍是 v1-v4 Runtime path | A2 -> C1 |
| backend-neutral renderer | 未实现；Player/adapter 仍直接走 OpenGL/SDL | A2 -> D1 -> D2 |
| player support/config/device transaction | 未实现；内部 target 和统一 command layer 不存在 | A2 -> C2/C3 |
| common media import | 未实现；固定 decoder、预算和 golden 未建立 | A2 -> E1/E2 |
| version enforcement / package flavor / Reference Host | 未实现；本次只做 consistency check | A2 -> B1/C4 |

本批次没有发现需要修改 ADR 0042 的复现；没有扩大 R5 candidate 清单、修改 golden、
提升 SDK API、建立稳定 C ABI、实现运行时脚本或创建私有 Runtime 路径。

## 6. 退出结论

S6-A1 退出条件已满足：基线已固定，入口调用链已盘点，R5 支持矩阵已落表，Debug/Release
以及现有 architecture/package/external consumer 门禁已实际运行，Foundation candidate CXC
在 developer-tools OFF/ON 配置中的注册差异已实测记录，所有缺口均已归属后续子批次。

因此 Stage 6 计划表中的 A1 标记为 `completed`，允许进入 S6-A2。A2 必须先形成
Spec/Schema、API 草案、依赖图、配置/媒体合同和独立表征/golden，不能把本报告中的
Foundation candidate CXC 测试当作 v5 Playback 实现。本报告不表示 Stage 6、A2 或 owner
acceptance 已完成；后续批次仍按计划依赖图逐批推进。
